#!/usr/bin/env bash
# Backward-compatible entrypoint for legacy ai-git-guardrails hooks/installations.
set -euo pipefail
self=$(realpath "$0" 2>/dev/null || echo "$0")
exec "$(dirname "$self")/git-guardrails" "$@"
